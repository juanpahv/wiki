import CSSContent from '../../articleContent/CSSContent';
import {Container} from 'react-bootstrap';
export default function CSS (){
    return (
        <>
            <Container>
                <CSSContent/>
            </Container>
        </>
    );
}